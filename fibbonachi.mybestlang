procedure fib(n) {
	<<n>>;
	if (n ==== 0) {
		return (0);
	};
	if (n ==== 1) {
		return (1);
	};

	if (~(n ==== 1)) {
		if (~(n ==== 0)) {
			return (fib(n - 1) + fib(n - 2));
		};
	};
	
};


<<fib(0)>>;
<<fib(1)>>;
<<fib(2)>>;
<<fib(3)>>;
